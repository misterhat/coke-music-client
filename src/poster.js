class Poster {
    constructor(game, room, { name, x, y }) {
        this.game = game;
        this.room = room;

        this.name = name;

        this.x = x;
        this.y = y;
    }

    update() {
    }

    draw() {
    }
}

module.exports = Poster;
